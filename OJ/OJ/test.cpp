#include <array>
#include <iostream>

using namespace std;


int main() {
    int n;
    cin >> n;
    char *_s;
    _s = (char *) malloc(512000 * sizeof(char));
    scanf("\n%[^\n]", _s);

    return 0;
}
